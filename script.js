// const card = document.querySelector(".section-card-container");

// let isDragging = false;
// const dragging = (e) => {
//   if (!isDragging) return;

//   console.log("draggin started");
//   card.scrollLeft -= e.movementX;
// };

// const dragStop = () => {
//   isDragging = false;
//   console.log("drag should stop");
// };
// card.addEventListener("mousemove", dragging);
// card.addEventListener("mousedown", () => (isDragging = true));
// document.addEventListener("mouseup", dragStop);
